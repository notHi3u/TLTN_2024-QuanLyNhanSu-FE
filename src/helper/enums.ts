// types/accountStatus.enum.ts
export enum AccountStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    SUSPENDED = 'suspended',
    DELETED = 'deleted',
}

export enum orderBy{
    ASC='ASC',
    DESC='DESC'
}
